export interface InitTimeConfig {
    month: boolean,
    day: boolean,
    hour: boolean,
    minute: boolean,
    seconds: boolean,
}

export interface TimeData {
    month: number,
    day: number,
    hour: number,
    minute: number,
    seconds: number,
}